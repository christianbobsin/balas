# Datasets

Este diretorio guarda datasets baixados localmente e nao versionados.

Uso atual:

- `datasets/downloads/`: arquivos compactados baixados pelos scripts
- `datasets/cifar-10-batches-py/`: extracao oficial do dataset CIFAR-10 em formato Python

O conteudo baixado aqui esta listado no `.gitignore`.
